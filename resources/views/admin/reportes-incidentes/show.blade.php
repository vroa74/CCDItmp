<x-app-layout><x-slot name="header"><h2 class="font-semibold text-xl text-gray-100">Detalle del reporte #{{ $reporte->id_reporte }}</h2></x-slot><div class="py-6"><div class="mx-auto max-w-5xl space-y-6 px-4 sm:px-6 lg:px-8">
    @if(session('message'))<div class="rounded-lg border border-green-700 bg-green-900/40 px-4 py-3 text-green-200">{{ session('message') }}</div>@endif
    <div class="rounded-2xl border border-gray-700 bg-gray-800 p-6 text-gray-200 shadow-xl"><div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><h3 class="text-xl font-semibold text-white">{{ str_replace('_', ' ', $reporte->tipo_falla) }}</h3><p class="text-sm text-gray-400">{{ $reporte->linea->edificio->nombre }} · Contrato {{ $reporte->linea->numero_contrato }}</p></div><a href="{{ route('reportes-incidentes.edit', $reporte) }}" class="rounded-lg bg-blue-600 px-4 py-2 text-sm text-white hover:bg-blue-700">Editar reporte</a></div><div class="mt-5 grid grid-cols-2 gap-4 text-sm md:grid-cols-4"><div><span class="block text-gray-400">Prioridad</span>{{ $reporte->prioridad }}</div><div><span class="block text-gray-400">Estatus</span>{{ str_replace('_', ' ', $reporte->estatus) }}</div><div><span class="block text-gray-400">Folio</span>{{ $reporte->folio_ticket_proveedor ?: 'Sin folio' }}</div><div><span class="block text-gray-400">Técnico</span>{{ $reporte->nombre_tecnico_proveedor ?: 'Sin técnico' }}</div><div><span class="block text-gray-400">Apertura</span>{{ $reporte->fecha_apertura->format('d/m/Y H:i') }}</div></div><p class="mt-5 whitespace-pre-line text-sm">{{ $reporte->descripcion_problema }}</p></div>
    <div class="rounded-2xl border border-gray-700 bg-gray-800 p-6 shadow-xl"><div class="flex items-center justify-between"><div><h3 class="text-lg font-semibold text-white">Seguimiento del reporte</h3><p class="text-sm text-gray-400">Este reporte tiene {{ $reporte->seguimientos->count() }} subreporte(s) de seguimiento.</p></div><a href="{{ route('reportes-incidentes.seguimientos.create', $reporte) }}" class="inline-flex items-center gap-2 rounded-lg bg-emerald-600 px-4 py-2 text-sm text-white hover:bg-emerald-700"><x-lucide name="plus" class="h-4 w-4" />Agregar seguimiento</a></div><div class="mt-5 space-y-4">@forelse($reporte->seguimientos as $seguimiento)<article class="rounded-lg border border-gray-700 bg-gray-900/60 p-4"><div class="flex justify-between gap-3"><div><p class="font-medium text-white">{{ $seguimiento->fecha_registro->format('d/m/Y H:i') }}</p><p class="text-xs text-gray-400">{{ $seguimiento->folio_subreporte_proveedor ?: 'Sin folio' }} · {{ $seguimiento->nombre_tecnico_proveedor ?: 'Sin técnico' }}</p></div><div><a href="{{ route('reportes-incidentes.seguimientos.edit', [$reporte, $seguimiento]) }}" class="mr-3 text-blue-400" title="Editar"><x-lucide name="pencil" class="inline h-4 w-4" /></a><form method="POST" action="{{ route('reportes-incidentes.seguimientos.destroy', [$reporte, $seguimiento]) }}" class="inline" onsubmit="return confirm('¿Deseas eliminar este seguimiento?')">@csrf @method('DELETE')<button type="submit" class="text-red-400" title="Eliminar"><x-lucide name="trash-2" class="inline h-4 w-4" /></button></form></div></div><p class="mt-3 whitespace-pre-line text-sm text-gray-300">{{ $seguimiento->comentarios }}</p>@if($seguimiento->proxima_accion)<p class="mt-2 text-xs text-cyan-300">Próxima acción: {{ $seguimiento->proxima_accion }}</p>@endif</article>@empty<p class="py-8 text-center text-gray-400">Aún no hay seguimientos registrados.</p>@endforelse</div></div>
</div></div></x-app-layout>